import { VehicleInformation } from './vehicle-information';

describe('VehicleInformation', () => {
  it('should create an instance', () => {
    expect(new VehicleInformation()).toBeTruthy();
  });
});
