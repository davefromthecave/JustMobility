import { RideMetaInformation } from './ride-meta-information';

describe('RideMetaInformation', () => {
  it('should create an instance', () => {
    expect(new RideMetaInformation()).toBeTruthy();
  });
});
